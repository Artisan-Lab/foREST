import  org.junit.AfterClass;
import  org.junit.BeforeClass;
import  org.junit.Before;
import  org.junit.Test;
import static org.junit.Assert.*;
import  java.util.Map;
import  io.restassured.RestAssured;
import static io.restassured.RestAssured.given;
import  io.restassured.response.ValidatableResponse;
import static org.evomaster.client.java.controller.api.EMTestUtils.*;
import  org.evomaster.client.java.controller.SutHandler;
import static org.evomaster.client.java.controller.db.dsl.SqlDsl.sql;
import  org.evomaster.client.java.controller.api.dto.database.operations.InsertionResultsDto;
import  org.evomaster.client.java.controller.api.dto.database.operations.InsertionDto;
import  java.util.List;
import static org.hamcrest.Matchers.*;
import  io.restassured.config.JsonConfig;
import  io.restassured.path.json.config.JsonPathConfig;
import static io.restassured.config.RedirectConfig.redirectConfig;
import static org.evomaster.client.java.controller.contentMatchers.NumberMatcher.*;
import static org.evomaster.client.java.controller.contentMatchers.StringMatcher.*;
import static org.evomaster.client.java.controller.contentMatchers.SubStringMatcher.*;
import static org.evomaster.client.java.controller.expect.ExpectationHandler.expectationHandler;
import  org.evomaster.client.java.controller.expect.ExpectationHandler;
import  io.restassured.path.json.JsonPath;
import  java.util.Arrays;




/**
 * This file was automatically generated by EvoMaster on 2022-01-13T00:37:23.429050600+08:00[Asia/Shanghai]
 * <br>
 * The generated test suite contains 67 tests
 * <br>
 * Covered targets: 106
 * <br>
 * Used time: 6h 0m 0s
 * <br>
 * Needed budget for current results: 99%
 * <br>
 * This file contains test cases that are likely to indicate faults.
 */
public class EvoMaster_faults_Test {

    
    private static String baseUrlOfSut = "http://192.168.112.186";
    /** [ems] - expectations master switch - is the variable that activates/deactivates expectations individual test cases
    * by default, expectations are turned off. The variable needs to be set to [true] to enable expectations
    */
    private static boolean ems = false;
    /**
    * sco - supported code oracle - checking that the response status code is among those supported according to the schema
    */
    private static boolean sco = false;
    
    
    @BeforeClass
    public static void initClass() {
        RestAssured.enableLoggingOfRequestAndResponseIfValidationFails();
        RestAssured.useRelaxedHTTPSValidation();
        RestAssured.urlEncodingEnabled = false;
        RestAssured.config = RestAssured.config()
            .jsonConfig(JsonConfig.jsonConfig().numberReturnType(JsonPathConfig.NumberReturnType.DOUBLE))
            .redirect(redirectConfig().followRedirects(false));
    }
    
    
    
    
    
    
    
    
    /**
    * [test_0_with500] is a part of 1 or more clusters, as defined by the selected clustering options. 
    * ErrorText_0
    */
    @Test
    public void test_0_with500() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups?" + 
                    "skip_groups=%5B%5D&" + 
                    "all_available=false&" + 
                    "search=qzHpw9ic35lw&" + 
                    "statistics=true&" + 
                    "with_custom_attributes=true")
                .then()
                .statusCode(500)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("500 Internal Server Error"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    /**
    * [test_1_with500] is a part of 1 or more clusters, as defined by the selected clustering options. 
    * ErrorText_0
    */
    @Test
    public void test_1_with500() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/60/subgroups?" + 
                    "all_available=false&" + 
                    "statistics=true&" + 
                    "with_custom_attributes=false&" + 
                    "owned=false")
                .then()
                .statusCode(500)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("500 Internal Server Error"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"push_events\": false, " + 
                    " \"issues_events\": false, " + 
                    " \"note_events\": true, " + 
                    " \"job_events\": false, " + 
                    " \"enable_ssl_verification\": false, " + 
                    " \"token\": \"r42jQTS4eWUjb\" " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/groups/62/hooks/256?url=rt")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
    }
    
    
    /**
    * [test_2_with500] is a part of 1 or more clusters, as defined by the selected clustering options. 
    * ErrorText_0
    */
    @Test
    public void test_2_with500() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/330")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"name\": \"nrimxB2DKSNyz\", " + 
                    " \"membership_lock\": true, " + 
                    " \"share_with_group_lock\": true, " + 
                    " \"require_two_factor_authentication\": false, " + 
                    " \"two_factor_grace_period\": 148, " + 
                    " \"auto_devops_enabled\": true, " + 
                    " \"lfs_enabled\": false, " + 
                    " \"request_access_enabled\": true, " + 
                    " \"parent_id\": -366617834 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups?" + 
                    "path=MqQ_nHlCLuNiJNj&" + 
                    "name=nrimxB2DKSNyz")
                .then()
                .statusCode(500)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("500 Internal Server Error"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/454/projects/shared?" + 
                    "archived=false&" + 
                    "search=tCri10&" + 
                    "simple=false&" + 
                    "min_access_level=501")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
        
        ValidatableResponse res_3 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"expires_at\": \"BVtijFzMge\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups/152/share?" + 
                    "group_id=937&" + 
                    "group_access=334")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("group_access does not have a valid value, expires_at is invalid"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_3.extract().statusCode()));
    }
    
    
    @Test
    public void test_3() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups?" + 
                    "skip_groups=%5B754%2C+874%2C+790%2C+957%5D&" + 
                    "all_available=true&" + 
                    "search=Az7WwWRN9&" + 
                    "statistics=false")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("size()", equalTo(0));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_4() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/42/hooks")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("size()", equalTo(0));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_5() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/972/ldap_group_links/KFPH")
                .then()
                .statusCode(403)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("403 Forbidden"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/41?with_projects=true")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("'web_url'", containsString("http://gitlab.example.com/groups/dMVrBOpLFZDYvsO9"))
                .body("'name'", containsString("uQv9LRBk"))
                .body("'path'", containsString("dMVrBOpLFZDYvsO9"))
                .body("'description'", containsString("YY2mDgZPr"))
                .body("'visibility'", containsString("private"))
                .body("'share_with_group_lock'", equalTo(false))
                .body("'require_two_factor_authentication'", equalTo(false))
                .body("'two_factor_grace_period'", numberMatches(780.0))
                .body("'project_creation_level'", containsString("developer"))
                .body("'auto_devops_enabled'", equalTo(true))
                .body("'subgroup_creation_level'", containsString("maintainer"))
                .body("'emails_disabled'", nullValue())
                .body("'mentions_disabled'", equalTo(false))
                .body("'lfs_enabled'", equalTo(true))
                .body("'default_branch_protection'", numberMatches(2.0))
                .body("'avatar_url'", nullValue())
                .body("'request_access_enabled'", equalTo(true))
                .body("'full_name'", containsString("uQv9LRBk"))
                .body("'full_path'", containsString("dMVrBOpLFZDYvsO9"))
                .body("'created_at'", containsString("2022-01-12T12:06:14.942Z"))
                .body("'parent_id'", nullValue())
                .body("'ldap_cn'", nullValue())
                .body("'ldap_access'", nullValue())
                .body("'shared_with_groups'.size()", equalTo(0))
                .body("'runners_token'", containsString("cd3kuD1sx8f2STxaQcUz"))
                .body("'prevent_sharing_groups_outside_hierarchy'", equalTo(false))
                .body("'projects'.size()", equalTo(0))
                .body("'shared_projects'.size()", equalTo(0))
                .body("'shared_runners_minutes_limit'", numberMatches(522.0))
                .body("'extra_shared_runners_minutes_limit'", numberMatches(86.0))
                .body("'prevent_forking_outside_group'", nullValue());
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
    }
    
    
    @Test
    public void test_6() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/50/descendant_groups?" + 
                    "search=AyCBTC5BNlEgT3G&" + 
                    "statistics=false&" + 
                    "with_custom_attributes=false&" + 
                    "owned=true")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("size()", equalTo(0));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"url\": \"k1sgwXYZ\", " + 
                    " \"deployment_events\": true, " + 
                    " \"releases_events\": true, " + 
                    " \"enable_ssl_verification\": true, " + 
                    " \"token\": \"x5XGpGvIvB8\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups/460/hooks?url=k1sgwXYZ")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
    }
    
    
    @Test
    public void test_7() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/320?with_projects=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/67/subgroups?" + 
                    "skip_groups=%5B%5D&" + 
                    "all_available=false&" + 
                    "statistics=true&" + 
                    "with_custom_attributes=true&" + 
                    "owned=true")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("size()", equalTo(0));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/633/projects?" + 
                    "archived=false&" + 
                    "sort=jdgEu2Z2&" + 
                    "simple=false&" + 
                    "with_merge_requests_enabled=true&" + 
                    "with_shared=false&" + 
                    "with_security_reports=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("sort does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
        
        ValidatableResponse res_3 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/groups/539/ldap_group_links?" + 
                    "cn=aKEf83550SDOOo&" + 
                    "group_access=871&" + 
                    "provider=KvaOxRSvBfVH")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("group_access does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_3.extract().statusCode()));
    }
    
    
    @Test
    public void test_8() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/779?with_custom_attributes=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/704/hooks")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/51/projects?" + 
                    "archived=true&" + 
                    "with_merge_requests_enabled=true&" + 
                    "with_shared=true&" + 
                    "with_custom_attributes=true")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("size()", equalTo(0));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
        
        ValidatableResponse res_3 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/groups/511/ldap_group_links/8GjRXDdjGQt")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_3.extract().statusCode()));
    }
    
    
    @Test
    public void test_9() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/616/subgroups?" + 
                    "all_available=true&" + 
                    "with_custom_attributes=true")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/230/hooks")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"issues_events\": true, " + 
                    " \"note_events\": true, " + 
                    " \"confidential_note_events\": false, " + 
                    " \"job_events\": true, " + 
                    " \"pipeline_events\": true, " + 
                    " \"deployment_events\": true, " + 
                    " \"releases_events\": false, " + 
                    " \"enable_ssl_verification\": true " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/groups/96/hooks/180?url=BZZ")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
        
        ValidatableResponse res_3 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/599/subgroups?" + 
                    "skip_groups=%5B949%2C+158%5D&" + 
                    "all_available=true&" + 
                    "sort=umMo3wT&" + 
                    "statistics=true&" + 
                    "with_custom_attributes=true")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("sort does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_3.extract().statusCode()));
        
        ValidatableResponse res_4 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/942/share/678")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_4.extract().statusCode()));
        
        ValidatableResponse res_5 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/55/projects/shared?" + 
                    "search=NyPa3NHia&" + 
                    "simple=false&" + 
                    "starred=true&" + 
                    "with_merge_requests_enabled=false&" + 
                    "with_custom_attributes=false")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("size()", equalTo(0));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_5.extract().statusCode()));
    }
    
    
    @Test
    public void test_10() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/901?with_projects=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/groups/837/restore")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/979/projects/shared?" + 
                    "archived=false&" + 
                    "visibility=VlffO&" + 
                    "sort=9VRy7&" + 
                    "search=fsuCtf9HON4Nw&" + 
                    "simple=true&" + 
                    "starred=false&" + 
                    "with_merge_requests_enabled=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("visibility does not have a valid value, sort does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
        
        ValidatableResponse res_3 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"name\": \"aaHn3lb7Dp\", " + 
                    " \"description\": \"WGtYUrPQ2xDx8mvf\", " + 
                    " \"share_with_group_lock\": false, " + 
                    " \"request_access_enabled\": false, " + 
                    " \"file_template_project_id\": 691, " + 
                    " \"extra_shared_runners_minutes_limit\": 472 " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/groups/68")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("'web_url'", containsString("http://gitlab.example.com/groups/wwbGJnnSHyWjxr"))
                .body("'name'", containsString("aaHn3lb7Dp"))
                .body("'path'", containsString("wwbGJnnSHyWjxr"))
                .body("'description'", containsString("WGtYUrPQ2xDx8mvf"))
                .body("'visibility'", containsString("private"))
                .body("'share_with_group_lock'", equalTo(false))
                .body("'require_two_factor_authentication'", equalTo(false))
                .body("'two_factor_grace_period'", numberMatches(23.0))
                .body("'project_creation_level'", containsString("developer"))
                .body("'auto_devops_enabled'", equalTo(true))
                .body("'subgroup_creation_level'", containsString("maintainer"))
                .body("'emails_disabled'", equalTo(true))
                .body("'mentions_disabled'", equalTo(true))
                .body("'lfs_enabled'", equalTo(false))
                .body("'default_branch_protection'", numberMatches(2.0))
                .body("'avatar_url'", nullValue())
                .body("'request_access_enabled'", equalTo(false))
                .body("'full_name'", containsString("aaHn3lb7Dp"))
                .body("'full_path'", containsString("wwbGJnnSHyWjxr"))
                .body("'created_at'", containsString("2022-01-12T15:35:25.384Z"))
                .body("'parent_id'", nullValue())
                .body("'ldap_cn'", nullValue())
                .body("'ldap_access'", nullValue())
                .body("'shared_with_groups'.size()", equalTo(0))
                .body("'prevent_sharing_groups_outside_hierarchy'", equalTo(false))
                .body("'projects'.size()", equalTo(0))
                .body("'shared_projects'.size()", equalTo(0))
                .body("'shared_runners_minutes_limit'", nullValue())
                .body("'extra_shared_runners_minutes_limit'", numberMatches(472.0))
                .body("'prevent_forking_outside_group'", nullValue());
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_3.extract().statusCode()));
        
        ValidatableResponse res_4 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/377/ldap_group_links?" + 
                    "cn=SdexlS5AaKIDZb&" + 
                    "filter=rdU&" + 
                    "provider=RjAlo7DU")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("cn, filter are mutually exclusive"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_4.extract().statusCode()));
        
        ValidatableResponse res_5 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/696")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_5.extract().statusCode()));
        
        ValidatableResponse res_6 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/748/descendant_groups?" + 
                    "skip_groups=%5B220%2C+286%2C+40%2C+337%5D&" + 
                    "order_by=edfeXdWqNEH&" + 
                    "owned=false&" + 
                    "min_access_level=655")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("order_by does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_6.extract().statusCode()));
        
        ValidatableResponse res_7 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/groups/635/ldap_group_links?" + 
                    "cn=3Uv6J6rj4YUxZ0U&" + 
                    "group_access=459&" + 
                    "provider=fEb9W")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("group_access does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_7.extract().statusCode()));
        
        ValidatableResponse res_8 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/groups/630/projects/798")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_8.extract().statusCode()));
    }
    
    
    @Test
    public void test_11() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"description\": \"BUMajG\", " + 
                    " \"membership_lock\": true, " + 
                    " \"share_with_group_lock\": false, " + 
                    " \"two_factor_grace_period\": 187, " + 
                    " \"auto_devops_enabled\": false, " + 
                    " \"emails_disabled\": false, " + 
                    " \"shared_runners_minutes_limit\": 510 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups?" + 
                    "path=KKPxA&" + 
                    "name=3XJs4SLNE_7w")
                .then()
                .statusCode(201)
                .assertThat()
                .contentType("application/json")
                .body("'web_url'", containsString("http://gitlab.example.com/groups/KKPxA"))
                .body("'name'", containsString("3XJs4SLNE_7w"))
                .body("'path'", containsString("KKPxA"))
                .body("'description'", containsString("BUMajG"))
                .body("'visibility'", containsString("private"))
                .body("'share_with_group_lock'", equalTo(false))
                .body("'require_two_factor_authentication'", equalTo(false))
                .body("'two_factor_grace_period'", numberMatches(187.0))
                .body("'project_creation_level'", containsString("developer"))
                .body("'auto_devops_enabled'", equalTo(false))
                .body("'subgroup_creation_level'", containsString("maintainer"))
                .body("'emails_disabled'", equalTo(false))
                .body("'mentions_disabled'", nullValue())
                .body("'lfs_enabled'", equalTo(true))
                .body("'default_branch_protection'", numberMatches(2.0))
                .body("'avatar_url'", nullValue())
                .body("'request_access_enabled'", equalTo(true))
                .body("'full_name'", containsString("3XJs4SLNE_7w"))
                .body("'full_path'", containsString("KKPxA"))
                .body("'created_at'", containsString("2022-01-12T15:23:27.565Z"))
                .body("'parent_id'", nullValue())
                .body("'ldap_cn'", nullValue())
                .body("'ldap_access'", nullValue())
                .body("'shared_with_groups'.size()", equalTo(0))
                .body("'prevent_sharing_groups_outside_hierarchy'", equalTo(false))
                .body("'projects'.size()", equalTo(0))
                .body("'shared_projects'.size()", equalTo(0))
                .body("'shared_runners_minutes_limit'", numberMatches(510.0))
                .body("'extra_shared_runners_minutes_limit'", nullValue())
                .body("'prevent_forking_outside_group'", nullValue());
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_12() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/53")
                .then()
                .statusCode(202)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("202 Accepted"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_13() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"expires_at\": \"QN1eoNmFn\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups/541/share?" + 
                    "group_id=902&" + 
                    "group_access=94")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("group_access does not have a valid value, expires_at is invalid"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_14() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"name\": \"c3GncD\", " + 
                    " \"membership_lock\": true, " + 
                    " \"require_two_factor_authentication\": true, " + 
                    " \"two_factor_grace_period\": 645, " + 
                    " \"project_creation_level\": \"\", " + 
                    " \"emails_disabled\": false, " + 
                    " \"mentions_disabled\": true, " + 
                    " \"request_access_enabled\": true, " + 
                    " \"parent_id\": 318, " + 
                    " \"default_branch_protection\": 776, " + 
                    " \"extra_shared_runners_minutes_limit\": 731, " + 
                    " \"shared_runners_setting\": \"kTsh\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups?" + 
                    "path=SsJkxSFUofcz0mFm&" + 
                    "name=c3GncD")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("project_creation_level does not have a valid value, default_branch_protection does not have a valid value, shared_runners_setting does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_15() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/198/projects/shared?" + 
                    "order_by=ifZq6CyFyjPbbiQX&" + 
                    "search=SCx6mLtaxdQ&" + 
                    "min_access_level=155&" + 
                    "with_custom_attributes=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("order_by does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_16() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"name\": \"ktQ1nSv3J\", " + 
                    " \"description\": \"8qul0hUXBk9oi4\", " + 
                    " \"membership_lock\": true, " + 
                    " \"visibility\": \"5Gz6\", " + 
                    " \"two_factor_grace_period\": 60, " + 
                    " \"emails_disabled\": true, " + 
                    " \"request_access_enabled\": true, " + 
                    " \"default_branch_protection\": 299, " + 
                    " \"shared_runners_minutes_limit\": 308 " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/groups/809")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("visibility does not have a valid value, default_branch_protection does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_17() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups?" + 
                    "skip_groups=%5B689%2C+319%2C+539%2C+585%5D&" + 
                    "all_available=false&" + 
                    "sort=QN3kCQrKX3jx62m_")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("sort does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_18() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/groups/731/ldap_group_links?" + 
                    "cn=HfTwEFeDr&" + 
                    "group_access=607&" + 
                    "provider=PkZtCS")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("group_access does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_19() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/980/subgroups?" + 
                    "all_available=false&" + 
                    "order_by=Ky7&" + 
                    "sort=qhW4q1QsXVJM&" + 
                    "min_access_level=78")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("order_by does not have a valid value, sort does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_20() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/485/projects?" + 
                    "archived=false&" + 
                    "visibility=Sth9rQ7g&" + 
                    "order_by=cX&" + 
                    "sort=oJK_UKbqQ4CcDTdm&" + 
                    "owned=false&" + 
                    "with_issues_enabled=false&" + 
                    "min_access_level=164&" + 
                    "with_security_reports=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("visibility does not have a valid value, order_by does not have a valid value, sort does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_21() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/230/descendant_groups?" + 
                    "all_available=true&" + 
                    "search=onJCJukz25KX&" + 
                    "order_by=1Ox&" + 
                    "statistics=false&" + 
                    "with_custom_attributes=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("order_by does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_22() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/869/ldap_group_links?" + 
                    "cn=50L3pWfy2nPYZegc&" + 
                    "filter=20g8H&" + 
                    "provider=Q1")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("cn, filter are mutually exclusive"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_23() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/groups/423/ldap_group_links?" + 
                    "cn=oN9OZIjKagWRfL&" + 
                    "group_access=10&" + 
                    "provider=mE35H70")
                .then()
                .statusCode(403)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("403 Forbidden"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups?" + 
                    "skip_groups=%5B571%2C+988%5D&" + 
                    "all_available=false&" + 
                    "sort=btlLjBtD&" + 
                    "statistics=false&" + 
                    "with_custom_attributes=true&" + 
                    "owned=false&" + 
                    "min_access_level=774&" + 
                    "top_level_only=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("sort does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
    }
    
    
    @Test
    public void test_24() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/42/ldap_group_links")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("No linked LDAP groups found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"expires_at\": \"zYIKtHikZsitz6z\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups/499/share?" + 
                    "group_id=1000&" + 
                    "group_access=922")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("group_access does not have a valid value, expires_at is invalid"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/587/descendant_groups?" + 
                    "skip_groups=%5B871%5D&" + 
                    "all_available=false&" + 
                    "owned=true&" + 
                    "min_access_level=672")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
    }
    
    
    @Test
    public void test_25() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/110/projects?" + 
                    "archived=true&" + 
                    "order_by=AF&" + 
                    "sort=fD2urW4QuH&" + 
                    "simple=false&" + 
                    "with_issues_enabled=false&" + 
                    "include_subgroups=true&" + 
                    "min_access_level=1656877574&" + 
                    "with_custom_attributes=true&" + 
                    "with_security_reports=true")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("order_by does not have a valid value, sort does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"url\": \"C3Ry0KFtDTnV7hJn\", " + 
                    " \"push_events\": false, " + 
                    " \"issues_events\": true, " + 
                    " \"confidential_issues_events\": true, " + 
                    " \"merge_requests_events\": false, " + 
                    " \"confidential_note_events\": true, " + 
                    " \"job_events\": true, " + 
                    " \"pipeline_events\": false, " + 
                    " \"releases_events\": true, " + 
                    " \"enable_ssl_verification\": false " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups/255/hooks?url=C3Ry0KFtDTnV7hJn")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups?" + 
                    "sort=AlJDtJ6ffS&" + 
                    "statistics=false&" + 
                    "with_custom_attributes=true&" + 
                    "owned=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("sort does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
        
        ValidatableResponse res_3 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" {} ")
                .post(baseUrlOfSut + "/api/v4/groups/377/share?" + 
                    "group_id=-1488149083&" + 
                    "group_access=30")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_3.extract().statusCode()));
    }
    
    
    @Test
    public void test_26() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .post(baseUrlOfSut + "/api/v4/groups/572/ldap_group_links?" + 
                    "cn=&" + 
                    "group_access=526&" + 
                    "provider=SyLxeBF1CZtVBxm")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_27() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/groups/400")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_28() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .post(baseUrlOfSut + "/api/v4/groups/526/restore")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_29() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"name\": \"5ju\", " + 
                    " \"description\": \"lUvUxKcfc\", " + 
                    " \"require_two_factor_authentication\": true, " + 
                    " \"subgroup_creation_level\": \"rG3CaIb9xpbiVJ\", " + 
                    " \"emails_disabled\": true, " + 
                    " \"avatar\": \"OqVGQatrAmbU7\", " + 
                    " \"mentions_disabled\": false, " + 
                    " \"request_access_enabled\": false, " + 
                    " \"shared_runners_minutes_limit\": 122, " + 
                    " \"prevent_forking_outside_group\": false " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/groups/753")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_30() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"url\": \"aZ9Ce2WUtMv\", " + 
                    " \"push_events\": false, " + 
                    " \"issues_events\": false, " + 
                    " \"merge_requests_events\": true, " + 
                    " \"tag_push_events\": true, " + 
                    " \"note_events\": false, " + 
                    " \"confidential_note_events\": true, " + 
                    " \"job_events\": true, " + 
                    " \"wiki_page_events\": false, " + 
                    " \"releases_events\": false, " + 
                    " \"token\": \"3qU6IRhz3o4L\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups/597/hooks?url=aZ9Ce2WUtMv")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_31() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/groups/1174123530/share/518")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_32() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/groups/240/ldap_group_links/E2vrZEKXXK8GjzWx/3aPuSmZHpOv")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_33() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/groups/201/ldap_group_links/iR09zYfxOxJ8mWY")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_34() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .get(baseUrlOfSut + "/api/v4/groups/437/ldap_group_links")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_35() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/groups/535/ldap_group_links?" + 
                    "cn=7icjaxFCAVWkh2&" + 
                    "filter=4c6Gcs8rQmO&" + 
                    "provider=KtBhygEI")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_36() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"description\": \"mOYgBo7wDPgmt\", " + 
                    " \"membership_lock\": false, " + 
                    " \"emails_disabled\": true, " + 
                    " \"lfs_enabled\": false, " + 
                    " \"parent_id\": 188, " + 
                    " \"default_branch_protection\": 263, " + 
                    " \"shared_runners_minutes_limit\": 340, " + 
                    " \"extra_shared_runners_minutes_limit\": 567, " + 
                    " \"shared_runners_setting\": \"UKiQTruNkxt\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups?" + 
                    "path=Tr&" + 
                    "name=ff27rMO3")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_37() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .contentType("application/json")
                .body(" {} ")
                .post(baseUrlOfSut + "/api/v4/groups/956/share?" + 
                    "group_id=281&" + 
                    "group_access=928")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_38() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"push_events\": false, " + 
                    " \"confidential_issues_events\": true, " + 
                    " \"tag_push_events\": true, " + 
                    " \"confidential_note_events\": false, " + 
                    " \"enable_ssl_verification\": true " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/groups/383/hooks/57?url=NDmjYWekk")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_39() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/groups/662/hooks/196")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_40() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .post(baseUrlOfSut + "/api/v4/groups/555/projects/503")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_41() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .get(baseUrlOfSut + "/api/v4/groups/736/hooks")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_42() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .get(baseUrlOfSut + "/api/v4/groups/968/hooks/597")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_43() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/369/ldap_group_links/HH_mnDnRtpPtp/7")
                .then()
                .statusCode(403)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("403 Forbidden"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_44() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/971/ldap_group_links?" + 
                    "cn=rh&" + 
                    "provider=WoaPeecRBnC")
                .then()
                .statusCode(403)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("403 Forbidden"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_45() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/444/ldap_group_links")
                .then()
                .statusCode(403)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("403 Forbidden"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_46() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/127/ldap_group_links/_44W")
                .then()
                .statusCode(403)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("403 Forbidden"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_47() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"url\": \"f5J9QS53\", " + 
                    " \"issues_events\": false, " + 
                    " \"merge_requests_events\": false, " + 
                    " \"confidential_note_events\": true, " + 
                    " \"pipeline_events\": true, " + 
                    " \"wiki_page_events\": true " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups/920/hooks?url=f5J9QS53")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_48() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/962/hooks/443")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_49() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/238")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_50() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"push_events\": false, " + 
                    " \"confidential_issues_events\": false, " + 
                    " \"merge_requests_events\": true, " + 
                    " \"tag_push_events\": true, " + 
                    " \"job_events\": true, " + 
                    " \"wiki_events\": true, " + 
                    " \"releases_events\": false, " + 
                    " \"enable_ssl_verification\": true, " + 
                    " \"token\": \"ZWVBCOy\" " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/groups/816/hooks/134?url=")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_51() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/164?" + 
                    "with_custom_attributes=true&" + 
                    "with_projects=true")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_52() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/263/hooks/315")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_53() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/943/hooks")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_54() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/groups/208/restore")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_55() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/groups/748/projects/616")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_56() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/631/share/169")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_57() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"name\": \"\", " + 
                    " \"require_two_factor_authentication\": true, " + 
                    " \"two_factor_grace_period\": 30, " + 
                    " \"mentions_disabled\": false, " + 
                    " \"lfs_enabled\": false, " + 
                    " \"parent_id\": 835, " + 
                    " \"shared_runners_minutes_limit\": 982, " + 
                    " \"extra_shared_runners_minutes_limit\": 969 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups?" + 
                    "path=HQZzVu1L&" + 
                    "name=")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_58() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/301/subgroups?" + 
                    "skip_groups=%5B399%2C+530%5D&" + 
                    "search=Udk6kNHAQ&" + 
                    "statistics=true&" + 
                    "with_custom_attributes=false&" + 
                    "owned=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_59() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/624/descendant_groups?statistics=true")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_60() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/870/projects?" + 
                    "search=vzdBnDnC&" + 
                    "owned=false&" + 
                    "starred=false&" + 
                    "with_issues_enabled=true&" + 
                    "with_shared=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_61() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"share_with_group_lock\": false, " + 
                    " \"require_two_factor_authentication\": true, " + 
                    " \"two_factor_grace_period\": 408, " + 
                    " \"auto_devops_enabled\": false, " + 
                    " \"emails_disabled\": true, " + 
                    " \"lfs_enabled (optional)\": false, " + 
                    " \"shared_runners_minutes_limit\": 960, " + 
                    " \"extra_shared_runners_minutes_limit\": 334, " + 
                    " \"prevent_forking_outside_group\": true " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/groups/309")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_62() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/groups/238/projects/shared?" + 
                    "simple=true&" + 
                    "with_custom_attributes=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_63() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/38/ldap_group_links/Y19L7hgZbaSel/HiyXFx")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("Linked LDAP group not found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_64() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/60/ldap_group_links?" + 
                    "cn=eFpWjANxKJ5wzz&" + 
                    "provider=uvOROIPeHMn")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("Linked LDAP group not found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_65() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/38/ldap_group_links/_i")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("Linked LDAP group not found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("*/*")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/groups/634")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Group Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
    }
    
    
    @Test
    public void test_66() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer TS2zsbuMVNmtjf3P3Cwx") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"url\": \"CmYRh0gR5JtqJ\", " + 
                    " \"issues_events\": false, " + 
                    " \"confidential_issues_events\": false, " + 
                    " \"note_events\": true, " + 
                    " \"pipeline_events\": true, " + 
                    " \"wiki_page_events\": true, " + 
                    " \"deployment_events\": false, " + 
                    " \"enable_ssl_verification\": false, " + 
                    " \"token\": \"4vT9iuUEPPECjGZn\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/groups/38/hooks?url=CmYRh0gR5JtqJ")
                .then()
                .statusCode(422)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("Invalid url given"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }


}
