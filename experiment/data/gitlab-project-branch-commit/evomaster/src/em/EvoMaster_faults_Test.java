import  org.junit.AfterClass;
import  org.junit.BeforeClass;
import  org.junit.Before;
import  org.junit.Test;
import static org.junit.Assert.*;
import  java.util.Map;
import  io.restassured.RestAssured;
import static io.restassured.RestAssured.given;
import  io.restassured.response.ValidatableResponse;
import static org.evomaster.client.java.controller.api.EMTestUtils.*;
import  org.evomaster.client.java.controller.SutHandler;
import static org.evomaster.client.java.controller.db.dsl.SqlDsl.sql;
import  org.evomaster.client.java.controller.api.dto.database.operations.InsertionResultsDto;
import  org.evomaster.client.java.controller.api.dto.database.operations.InsertionDto;
import  java.util.List;
import static org.hamcrest.Matchers.*;
import  io.restassured.config.JsonConfig;
import  io.restassured.path.json.config.JsonPathConfig;
import static io.restassured.config.RedirectConfig.redirectConfig;
import static org.evomaster.client.java.controller.contentMatchers.NumberMatcher.*;
import static org.evomaster.client.java.controller.contentMatchers.StringMatcher.*;
import static org.evomaster.client.java.controller.contentMatchers.SubStringMatcher.*;
import static org.evomaster.client.java.controller.expect.ExpectationHandler.expectationHandler;
import  org.evomaster.client.java.controller.expect.ExpectationHandler;
import  io.restassured.path.json.JsonPath;
import  java.util.Arrays;




/**
 * This file was automatically generated by EvoMaster on 2022-01-06T20:12:48.408964300+08:00[Asia/Shanghai]
 * <br>
 * The generated test suite contains 87 tests
 * <br>
 * Covered targets: 140
 * <br>
 * Used time: 6h 0m 0s
 * <br>
 * Needed budget for current results: 89%
 * <br>
 * This file contains test cases that are likely to indicate faults.
 */
public class EvoMaster_faults_Test {

    
    private static String baseUrlOfSut = "http://192.168.112.176";
    /** [ems] - expectations master switch - is the variable that activates/deactivates expectations individual test cases
    * by default, expectations are turned off. The variable needs to be set to [true] to enable expectations
    */
    private static boolean ems = false;
    /**
    * sco - supported code oracle - checking that the response status code is among those supported according to the schema
    */
    private static boolean sco = false;
    
    
    @BeforeClass
    public static void initClass() {
        RestAssured.enableLoggingOfRequestAndResponseIfValidationFails();
        RestAssured.useRelaxedHTTPSValidation();
        RestAssured.urlEncodingEnabled = false;
        RestAssured.config = RestAssured.config()
            .jsonConfig(JsonConfig.jsonConfig().numberReturnType(JsonPathConfig.NumberReturnType.DOUBLE))
            .redirect(redirectConfig().followRedirects(false));
    }
    
    
    
    
    
    
    
    
    @Test
    public void test_0() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects?" + 
                    "id_after=932&" + 
                    "id_before=689&" + 
                    "membership=false&" + 
                    "owned=false&" + 
                    "repository_checksum_failed=false&" + 
                    "repository_storage=HoO9hF21lJd2bgdq&" + 
                    "search=Xi2thKry6fLqV6Er&" + 
                    "simple=false&" + 
                    "starred=true&" + 
                    "statistics=true&" + 
                    "visibility=internal&" + 
                    "with_issues_enabled=true&" + 
                    "with_merge_requests_enabled=false")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("size()", equalTo(0));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_1() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/813/repository/commits/OlB8xxdf")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/124/repository/commits/Zy9EY9Qy55gwG/statuses?" + 
                    "ref=r&" + 
                    "stage=dvvHz5qdSWu6kYp6&" + 
                    "all=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/projects/216/repository/merged_branches")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
        
        ValidatableResponse res_3 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"branch\": \"KrC\", " + 
                    " \"dry_run\": true " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/301/repository/commits/gNY3inlKV/cherry_pick")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_3.extract().statusCode()));
        
        ValidatableResponse res_4 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/users/1/projects?" + 
                    "id_after=785&" + 
                    "membership=true&" + 
                    "owned=true&" + 
                    "simple=true&" + 
                    "starred=true&" + 
                    "statistics=true&" + 
                    "visibility=internal&" + 
                    "with_issues_enabled=true&" + 
                    "with_programming_language=McSf4UeDIbm9FA")
                .then()
                .statusCode(200)
                .assertThat()
                .contentType("application/json")
                .body("size()", equalTo(0));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_4.extract().statusCode()));
    }
    
    
    @Test
    public void test_2() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"approvals_before_merge\": 1078213330, " + 
                    " \"auto_cancel_pending_pipelines\": \"pEib\", " + 
                    " \"avatar\": { " + 
                    " \"JL\": \"6rU_y_L88\", " + 
                    " \"QRQPpmy6sGM\": \"jCItNYXnS1e\", " + 
                    " \"7EWM\": \"uPN1HsN4dwfy\", " + 
                    " \"_gaftGcn4rV9JN\": \"0Kj\" " + 
                    " }, " + 
                    " \"build_git_strategy\": \"LLLl\", " + 
                    " \"container_registry_enabled\": false, " + 
                    " \"external_authorization_classification_label\": \"9inE\", " + 
                    " \"group_with_project_templates_id\": 229, " + 
                    " \"import_url\": \"xricZnRz\", " + 
                    " \"jobs_enabled\": true, " + 
                    " \"lfs_enabled\": false, " + 
                    " \"merge_requests_enabled\": false, " + 
                    " \"namespace_id\": 952, " + 
                    " \"only_allow_merge_if_all_discussions_are_resolved\": true, " + 
                    " \"repository_access_level\": \"k\", " + 
                    " \"repository_storage\": \"dLIXzw8wmjSUjwKq\", " + 
                    " \"resolve_outdated_diff_discussions\": false, " + 
                    " \"show_default_award_emojis\": false, " + 
                    " \"snippets_enabled\": false, " + 
                    " \"template_name\": \"_NVkRsP1jVB\", " + 
                    " \"visibility\": \"EVOMASTER\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/user/838?name=PMKc0Ygb")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("build_git_strategy does not have a valid value, auto_cancel_pending_pipelines does not have a valid value, repository_access_level does not have a valid value, visibility does not have a valid value, avatar is invalid, import_url, template_name are mutually exclusive"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_3() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects?" + 
                    "membership=true&" + 
                    "min_access_level=380&" + 
                    "order_by=gmCHG8r&" + 
                    "owned=false&" + 
                    "search_namespaces=true&" + 
                    "search=f&" + 
                    "simple=true&" + 
                    "starred=false&" + 
                    "wiki_checksum_failed=true&" + 
                    "with_custom_attributes=true&" + 
                    "with_merge_requests_enabled=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("order_by does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_4() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/users/451/projects?" + 
                    "archived=false&" + 
                    "id_after=914&" + 
                    "min_access_level=1436046908&" + 
                    "order_by=DM1Xoe7026&" + 
                    "sort=QQdH&" + 
                    "starred=true&" + 
                    "statistics=false&" + 
                    "visibility=internal&" + 
                    "with_issues_enabled=false&" + 
                    "with_programming_language=zheYrn2x")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("order_by does not have a valid value, sort does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_5() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/users/824/starred_projects?" + 
                    "archived=true&" + 
                    "membership=true&" + 
                    "min_access_level=692&" + 
                    "sort=x2n3KoAN&" + 
                    "starred=true&" + 
                    "statistics=true&" + 
                    "visibility=public&" + 
                    "with_issues_enabled=true")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("sort does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_6() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/824/groups?shared_min_access_level=57")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("shared_min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_7() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"approvals_before_merge\": 605, " + 
                    " \"auto_devops_deploy_strategy\": \"igDnMqm\", " + 
                    " \"auto_devops_enabled\": true, " + 
                    " \"autoclose_referenced_issues\": true, " + 
                    " \"build_coverage_regex\": \"mkpwBj\", " + 
                    " \"build_git_strategy\": \"MFNm6xMTyO\", " + 
                    " \"build_timeout\": 804, " + 
                    " \"builds_access_level\": \"NfAB_9icZqMZYj\", " + 
                    " \"ci_config_path\": \"DPEU9K5KVk_US\", " + 
                    " \"ci_default_git_depth\": 452, " + 
                    " \"ci_forward_deployment_enabled\": false, " + 
                    " \"default_branch\": \"RqhA\", " + 
                    " \"description\": \"QcHMg8lpeUFKki\", " + 
                    " \"forking_access_level\": \"B29AmE7hDgk\", " + 
                    " \"import_url\": \"X7LdEDusx\", " + 
                    " \"jobs_enabled\": false, " + 
                    " \"merge_method\": \"yiZyuZkNit59jLb0\", " + 
                    " \"merge_requests_access_level\": \"tbTApY\", " + 
                    " \"merge_requests_enabled\": true, " + 
                    " \"mirror_trigger_builds\": false, " + 
                    " \"only_allow_merge_if_all_discussions_are_resolved\": false, " + 
                    " \"only_allow_merge_if_pipeline_succeeds\": false, " + 
                    " \"packages_enabled\": false, " + 
                    " \"requirements_access_level\": \"IkWSzs\", " + 
                    " \"public_builds\": true, " + 
                    " \"remove_source_branch_after_merge\": true, " + 
                    " \"repository_access_level\": \"FsfQ\", " + 
                    " \"request_access_enabled\": true, " + 
                    " \"resolve_outdated_diff_discussions\": true, " + 
                    " \"service_desk_enabled\": true, " + 
                    " \"snippets_enabled\": false, " + 
                    " \"suggestion_commit_message\": \"gZO3_oT1\", " + 
                    " \"tag_list\": [ " + 
                    " { " + 
                    " \"iomxq0afkYOmz\": \"w_hn\" " + 
                    " } " + 
                    " ], " + 
                    " \"wiki_access_level\": \"hnYK\", " + 
                    " \"wiki_enabled\": false " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/projects/820")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("build_git_strategy does not have a valid value, repository_access_level does not have a valid value, merge_requests_access_level does not have a valid value, forking_access_level does not have a valid value, wiki_access_level does not have a valid value, builds_access_level does not have a valid value, merge_method does not have a valid value, auto_devops_deploy_strategy does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_8() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"group_access\": 623, " + 
                    " \"group_id\": 618 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/350/share?" + 
                    "group_id=618&" + 
                    "group_access=10")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("group_access does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_9() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/344/forks?" + 
                    "archived=false&" + 
                    "membership=true&" + 
                    "min_access_level=253&" + 
                    "simple=true&" + 
                    "sort=Zfl2Mp8rMMg&" + 
                    "visibility=internal")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("sort does not have a valid value, min_access_level does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_10() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"auto_cancel_pending_pipelines\": \"_q2wipA5v8Ag3Ow\", " + 
                    " \"auto_devops_deploy_strategy\": \"TZwLtC\", " + 
                    " \"avatar\": { " + 
                    " \"X7\": \"ePbZu9qWWdM5fUQ\", " + 
                    " \"N5J6TOG\": \"yFCycCMvC7\", " + 
                    " \"GWCY888SlgH\": \"exOQre82HcG2FVX\", " + 
                    " \"NcNvG24qEB6mj\": \"w36\" " + 
                    " }, " + 
                    " \"build_git_strategy\": \"B3_ER\", " + 
                    " \"ci_config_path\": \"l\", " + 
                    " \"container_expiration_policy_attributes\": { " + 
                    " \"ir6M3da\": \"jBUMlJZw\", " + 
                    " \"Ck1LQ\": \"C7ONZKIa\", " + 
                    " \"Y_j\": \"9YUm7cSZmOF\" " + 
                    " }, " + 
                    " \"container_registry_enabled\": true, " + 
                    " \"default_branch\": \"Bv5hKGwDPKSj\", " + 
                    " \"group_with_project_templates_id\": 939, " + 
                    " \"import_url\": \"Id6RCArDj\", " + 
                    " \"initialize_with_readme\": true, " + 
                    " \"issues_access_level\": \"gRL\", " + 
                    " \"issues_enabled\": false, " + 
                    " \"jobs_enabled\": false, " + 
                    " \"merge_method\": \"UD0wTvja0i5\", " + 
                    " \"merge_requests_enabled\": true, " + 
                    " \"mirror_trigger_builds\": true, " + 
                    " \"mirror\": true, " + 
                    " \"packages_enabled\": true, " + 
                    " \"path\": \"UOPyTsDHENF3l_8B\", " + 
                    " \"remove_source_branch_after_merge\": true, " + 
                    " \"repository_storage\": \"R4\", " + 
                    " \"snippets_access_level\": \"p0cKb1V\", " + 
                    " \"snippets_enabled\": true, " + 
                    " \"tag_list\": [ " + 
                    " { " + 
                    " \"lvVcE\": \"N8c9UyqYZ\", " + 
                    " \"kA4Wgn_VZQ0\": \"ZBUTx\", " + 
                    " \"_I\": \"xHBdG\", " + 
                    " \"ZdXSqvSCot1qp\": \"xgQbh4w\" " + 
                    " } " + 
                    " ], " + 
                    " \"template_name\": \"J3e50Xskq9K6u\", " + 
                    " \"visibility\": \"internal\", " + 
                    " \"wiki_access_level\": \"\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects?name=xsOx")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("build_git_strategy does not have a valid value, auto_cancel_pending_pipelines does not have a valid value, issues_access_level does not have a valid value, wiki_access_level does not have a valid value, snippets_access_level does not have a valid value, avatar is invalid, merge_method does not have a valid value, auto_devops_deploy_strategy does not have a valid value, import_url, template_name are mutually exclusive"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_11() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"state\": \"EVOMASTER\", " + 
                    " \"ref\": \"fY\", " + 
                    " \"name or context\": \"iX55BBgLOluwy\", " + 
                    " \"target_url\": \"\", " + 
                    " \"description\": \"u9kUT\", " + 
                    " \"coverage\": 0.13212556895638483 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/-1215034676/statuses/1ltZcoJTmbqhtN_5")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("state does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_12() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/585/repository/branches?search=P1a12gNOp")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/706/housekeeping")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
        
        ValidatableResponse res_2 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"approvals_before_merge\": 238, " + 
                    " \"auto_devops_enabled\": false, " + 
                    " \"autoclose_referenced_issues\": false, " + 
                    " \"container_registry_enabled\": true, " + 
                    " \"import_url\": \"Ja5Tyq\", " + 
                    " \"initialize_with_readme\": true, " + 
                    " \"issues_enabled\": true, " + 
                    " \"jobs_enabled\": true, " + 
                    " \"merge_requests_enabled\": true, " + 
                    " \"mirror_trigger_builds\": true, " + 
                    " \"mirror\": false, " + 
                    " \"namespace_id\": 192, " + 
                    " \"packages_enabled\": true, " + 
                    " \"path\": \"EcCoZntL\", " + 
                    " \"printing_merge_request_link_enabled\": false, " + 
                    " \"remove_source_branch_after_merge\": true, " + 
                    " \"repository_storage\": \"dlSkHz\", " + 
                    " \"request_access_enabled\": false, " + 
                    " \"show_default_award_emojis\": false, " + 
                    " \"snippets_enabled\": true " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/user/599?name=W3MEfzdNN")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 User Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_2.extract().statusCode()));
        
        ValidatableResponse res_3 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/projects/752/share/746")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_3.extract().statusCode()));
        
        ValidatableResponse res_4 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/761/repository/branches?" + 
                    "branch=4KiyqfWCZLFRjz&" + 
                    "ref=hMPjlnC5HvzLdo")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_4.extract().statusCode()));
        
        ValidatableResponse res_5 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"confidential_issues_events\": false, " + 
                    " \"note_events\": false, " + 
                    " \"pipeline_events\": true, " + 
                    " \"push_events_branch_filter\": \"v\", " + 
                    " \"push_events\": false, " + 
                    " \"tag_push_events\": true, " + 
                    " \"wiki_events\": true " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/projects/537/hooks/915?url=1nvaeysM5")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_5.extract().statusCode()));
        
        ValidatableResponse res_6 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/users/666/starred_projects?" + 
                    "archived=false&" + 
                    "membership=false&" + 
                    "owned=true&" + 
                    "sort=KGEJYMU6mGoY&" + 
                    "starred=false&" + 
                    "statistics=true&" + 
                    "visibility=EVOMASTER&" + 
                    "with_custom_attributes=false&" + 
                    "with_issues_enabled=false&" + 
                    "with_merge_requests_enabled=false")
                .then()
                .statusCode(400)
                .assertThat()
                .contentType("application/json")
                .body("'error'", containsString("sort does not have a valid value, visibility does not have a valid value"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_6.extract().statusCode()));
    }
    
    
    @Test
    public void test_13() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .post(baseUrlOfSut + "/api/v4/projects/651/archive")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_14() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .contentType("application/json")
                .body(" { " + 
                    " \"expires_at\": \"8Ot\", " + 
                    " \"group_access\": 39, " + 
                    " \"group_id\": 784 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/464/share?" + 
                    "group_id=784&" + 
                    "group_access=42")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_15() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .delete(baseUrlOfSut + "/api/v4/projects/836/hooks/933")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_16() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .post(baseUrlOfSut + "/api/v4/projects/498/unstar")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_17() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"auto_devops_deploy_strategy\": \"MTMqACmh8vzWDl\", " + 
                    " \"avatar\": { " + 
                    " \"Qy1yt\": \"n0hT8\" " + 
                    " }, " + 
                    " \"build_git_strategy\": \"SSE3itDz\", " + 
                    " \"builds_access_level\": \"qQwk\", " + 
                    " \"ci_config_path\": \"4s_LZ\", " + 
                    " \"emails_disabled\": true, " + 
                    " \"external_authorization_classification_label\": \"2ny3\", " + 
                    " \"initialize_with_readme\": false, " + 
                    " \"issues_access_level\": \"0mrmUK6DzUxeupd\", " + 
                    " \"merge_requests_enabled\": true, " + 
                    " \"mirror_trigger_builds\": true, " + 
                    " \"only_allow_merge_if_pipeline_succeeds\": true, " + 
                    " \"pages_access_level\": \"a\", " + 
                    " \"path\": \"t\", " + 
                    " \"printing_merge_request_link_enabled\": false, " + 
                    " \"remove_source_branch_after_merge\": true, " + 
                    " \"request_access_enabled\": false, " + 
                    " \"resolve_outdated_diff_discussions\": false, " + 
                    " \"shared_runners_enabled\": false, " + 
                    " \"suggestion_commit_message\": \"U81w5vxiO_GKyDR\", " + 
                    " \"tag_list\": [ " + 
                    " { " + 
                    " \"4B3EKL1HkAIwtGl\": \"WMqTgtBo\" " + 
                    " }, " + 
                    " { " + 
                    " \"vXytA2IqpEs\": \"uOpyM2E3f2\" " + 
                    " }, " + 
                    " { " + 
                    " \"S2GNuFmhr\": \"Jh1bCk5z1D\", " + 
                    " \"Pc6z38M9bKAtJX7\": \"Ev7DK\", " + 
                    " \"QEYSX6Sl_esQ\": \"xUBdKM_bGsY\" " + 
                    " }, " + 
                    " { " + 
                    " \"ssywi0SfBbpbiL\": \"SpYgl8l\" " + 
                    " } " + 
                    " ], " + 
                    " \"template_name\": \"gfTLs3nv51QBI0O\", " + 
                    " \"use_custom_template\": false, " + 
                    " \"visibility\": \"private\", " + 
                    " \"wiki_enabled\": false " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/user/112?name=yn")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_18() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .get(baseUrlOfSut + "/api/v4/projects/959/repository/commits/KhtwOGJ/discussions")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_19() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"allow_merge_on_skipped_pipeline\": true, " + 
                    " \"auto_cancel_pending_pipelines\": \"G\", " + 
                    " \"builds_access_level\": \"5\", " + 
                    " \"default_branch\": \"0fjd4H5Wux_ZJ\", " + 
                    " \"emails_disabled\": false, " + 
                    " \"group_with_project_templates_id\": 1352057313, " + 
                    " \"import_url\": \"KI6MHBbAObw\", " + 
                    " \"merge_method\": \"vf8Ibjvd\", " + 
                    " \"mirror\": false, " + 
                    " \"only_allow_merge_if_all_discussions_are_resolved\": true, " + 
                    " \"packages_enabled\": true, " + 
                    " \"requirements_access_level\": \"zTkoZSVDgf\", " + 
                    " \"printing_merge_request_link_enabled\": false, " + 
                    " \"repository_access_level\": \"iCq\", " + 
                    " \"repository_storage\": \"YBfM3EqIr1nNa\", " + 
                    " \"request_access_enabled\": true, " + 
                    " \"show_default_award_emojis\": true, " + 
                    " \"snippets_access_level\": \"f1Q1CCsAg2MqR\", " + 
                    " \"snippets_enabled\": true, " + 
                    " \"visibility\": \"internal\", " + 
                    " \"wiki_access_level\": \"9EbrQIn\", " + 
                    " \"wiki_enabled\": true " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects?name=")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_20() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .get(baseUrlOfSut + "/api/v4/projects/312/snapshot")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_21() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .get(baseUrlOfSut + "/api/v4/projects/554/hooks/47")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_22() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"file\": \"\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/696/uploads")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_23() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .post(baseUrlOfSut + "/api/v4/projects/825/star")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_24() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .get(baseUrlOfSut + "/api/v4/projects/786/repository/commits/TZ/statuses?" + 
                    "ref=ABL&" + 
                    "stage=WGAD_ncWYXnLMPf&" + 
                    "name=KinWyTJkqgNeUkG&" + 
                    "all=true")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_25() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"confidential_note_events\": true, " + 
                    " \"enable_ssl_verification\": true, " + 
                    " \"issues_events\": false, " + 
                    " \"merge_requests_events\": true, " + 
                    " \"push_events_branch_filter\": \"kYgkrO7VEeyR\", " + 
                    " \"push_events\": false " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/975/hooks?url=A9")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_26() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .contentType("application/json")
                .body(" { " + 
                    " \"name\": \"XFTfMaKz\", " + 
                    " \"namespace_id\": 889 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/221/fork?" + 
                    "path=PvSMQ5z&" + 
                    "name=XFTfMaKz")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_27() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .post(baseUrlOfSut + "/api/v4/projects/508/restore")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_28() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .get(baseUrlOfSut + "/api/v4/projects/927/hooks")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_29() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .get(baseUrlOfSut + "/api/v4/projects/162/storage")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_30() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"state\": \"success\", " + 
                    " \"name or context\": \"hwp\", " + 
                    " \"target_url\": \"YQyhMO\", " + 
                    " \"description\": \"mv4lMIVsySfVC\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/301/statuses/_vEzOhFJEcjwZo")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_31() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/projects/271")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_32() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"auto_cancel_pending_pipelines\": \"YvzRx_9bFfo\", " + 
                    " \"build_coverage_regex\": \"\", " + 
                    " \"build_timeout\": 718, " + 
                    " \"builds_access_level\": \"Fo\", " + 
                    " \"ci_config_path\": \"_igMqiW5XGe0Ev\", " + 
                    " \"ci_default_git_depth\": 824, " + 
                    " \"ci_forward_deployment_enabled\": true, " + 
                    " \"container_expiration_policy_attributes\": { " + 
                    " \"e37_cm0cremos\": \"dbI12c\", " + 
                    " \"FgPsNIjm\": \"XNjq\", " + 
                    " \"EXa\": \"wRlCqyf\", " + 
                    " \"8JXFfmCK40Y\": \"A\" " + 
                    " }, " + 
                    " \"default_branch\": \"aIXjhBRQjmoFzyL\", " + 
                    " \"emails_disabled\": false, " + 
                    " \"import_url\": \"9\", " + 
                    " \"issues_access_level\": \"I5J8MDOCTJ71z\", " + 
                    " \"jobs_enabled\": false, " + 
                    " \"lfs_enabled\": false, " + 
                    " \"merge_requests_access_level\": \"_nkI\", " + 
                    " \"merge_requests_enabled\": true, " + 
                    " \"mirror_trigger_builds\": true, " + 
                    " \"mirror_user_id\": 692, " + 
                    " \"mirror\": false, " + 
                    " \"only_mirror_protected_branches\": true, " + 
                    " \"pages_access_level\": \"lswdF9ymCd\", " + 
                    " \"requirements_access_level\": \"hmtYAD8nV\", " + 
                    " \"path\": \"2r5gYOu3r9\", " + 
                    " \"public_builds\": true, " + 
                    " \"remove_source_branch_after_merge\": false, " + 
                    " \"shared_runners_enabled\": false, " + 
                    " \"snippets_enabled\": true, " + 
                    " \"wiki_access_level\": \"JnCS\", " + 
                    " \"wiki_enabled\": false " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/projects/769")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_33() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/projects/564/share/180")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_34() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .post(baseUrlOfSut + "/api/v4/projects/981/unarchive")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_35() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .contentType("application/json")
                .body(" { " + 
                    " \"confidential_issues_events\": false, " + 
                    " \"issues_events\": false, " + 
                    " \"merge_requests_events\": false, " + 
                    " \"note_events\": false, " + 
                    " \"pipeline_events\": false, " + 
                    " \"push_events_branch_filter\": \"1UG\", " + 
                    " \"tag_push_events\": true, " + 
                    " \"token\": \"RDt8CWQpDIHDgZB\", " + 
                    " \"url\": \"3RwdfVwEx\", " + 
                    " \"wiki_events\": true " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/projects/288/hooks/208?url=3RwdfVwEx")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_36() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .post(baseUrlOfSut + "/api/v4/projects/12/fork/891")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_37() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .post(baseUrlOfSut + "/api/v4/projects/850/housekeeping")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_38() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .put(baseUrlOfSut + "/api/v4/projects/485/transfer?namespace=tsupkHeZE9eS")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_39() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .delete(baseUrlOfSut + "/api/v4/projects/901/fork")
                .then()
                .statusCode(401)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("401 Unauthorized"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_40() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/projects/44/fork")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_41() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/880/languages")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_42() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/224/repository/branches?search=tC")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_43() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"confidential_note_events\": true, " + 
                    " \"merge_requests_events\": false, " + 
                    " \"note_events\": false, " + 
                    " \"push_events_branch_filter\": \"crtQJ\", " + 
                    " \"push_events\": true, " + 
                    " \"token\": \"98gVxoRbXmCKNIWB\", " + 
                    " \"wiki_page_events\": true " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/880/hooks?url=hlBT87eYkf7uyA6D")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_44() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/745/archive")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_45() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/622/unstar")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_46() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .put(baseUrlOfSut + "/api/v4/projects/469/transfer?namespace=vcQmhME04I")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_47() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/732/repository/commits/3SzM0Rls3eSznMd8?stats=true")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_48() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"file\": \"Lu6xnjFYk\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/763/uploads")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/vnd.gitlab-workhorse+json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_49() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/-1585083439/repository/commits/WF8ULXaWiYKkDoJ/signature")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_50() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/419/hooks")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_51() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/639/star")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_52() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/users/712/starred_projects?" + 
                    "owned=true&" + 
                    "visibility=internal")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 User Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_53() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/projects/713/share/147")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_54() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/504/repository/commits/SvbyUVjKjUlI/refs?type=tag")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_55() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"state\": \"canceled\", " + 
                    " \"pipeline_id\": 336 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/339/statuses/o83Z3JYPWWK")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_56() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"branch\": \"2UcmA1r0xM\", " + 
                    " \"commit_message\": \"r9TVFO\", " + 
                    " \"start_branch\": \"Br_TkUCytezJiLS\", " + 
                    " \"start_sha\": \"UrS4\", " + 
                    " \"start_project\": \"cc\", " + 
                    " \"actions\": [], " + 
                    " \"author_email\": \"XB418d\", " + 
                    " \"force\": true " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/951/repository/commits")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_57() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/60/repository/commits/V/discussions")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_58() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/0/repository/commits/1D/statuses?" + 
                    "ref=ACrm&" + 
                    "stage=yNmZ_8O6ODmCyWa&" + 
                    "all=true")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_59() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/339/starrers")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_60() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/projects/125")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_61() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/527/groups?" + 
                    "skip_group=%5B758%5D&" + 
                    "with_shared=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_62() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"branch\": \"hEN\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/750/repository/commits/5aN7Vkdw1yK/revert")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_63() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/projects/410/repository/branches/OjKBXUxskhW1YNX")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_64() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/projects/445/repository/merged_branches")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_65() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/629/repository/branches?" + 
                    "branch=041kbeUZHd5&" + 
                    "ref=kGy2")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_66() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/1376917317/repository/commits/hqDGisK/diff")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_67() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"namespace_id\": 107 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/20/fork?" + 
                    "path=LtI_Bm&" + 
                    "name=E1DLdtmeUbKW8pnJ")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_68() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/691/repository/branches/5NO1Q")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_69() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/863/fork/342")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_70() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/41/users")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_71() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/569/unarchive")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_72() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/516/repository/commits?" + 
                    "ref_name=EUnW8LBvQbT&" + 
                    "until=Lw7Iy7EdI&" + 
                    "all=true&" + 
                    "order=3")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_73() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"branch\": \"Yr15aonz8yv\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/561/repository/commits/U1eTpCtq/cherry_pick")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_74() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"note\": \"UO\", " + 
                    " \"line_type\": \"qu4CGgB2\" " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/978/repository/commits/OBignIOG/comments")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_75() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .delete(baseUrlOfSut + "/api/v4/projects/768/hooks/560")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_76() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/664/restore")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_77() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/178/hooks/389")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_78() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/569/snapshot")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_79() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"confidential_issues_events\": true, " + 
                    " \"confidential_note_events\": false, " + 
                    " \"deployment_events\": false, " + 
                    " \"issues_events\": true, " + 
                    " \"merge_requests_events\": true, " + 
                    " \"push_events\": false, " + 
                    " \"tag_push_events\": true, " + 
                    " \"token\": \"cK\", " + 
                    " \"url\": \"uGsAM5Ro403Qmt2\" " + 
                    " } ")
                .put(baseUrlOfSut + "/api/v4/projects/106/hooks/34?url=uGsAM5Ro403Qmt2")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_80() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/965/repository/commits/GzBNZyEOFkARicb9/comments")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_81() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/users/76/projects?" + 
                    "archived=true&" + 
                    "id_before=936&" + 
                    "membership=false&" + 
                    "owned=false&" + 
                    "search=rHCCI&" + 
                    "with_issues_enabled=false&" + 
                    "with_programming_language=qpY9")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 User Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_82() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/297/storage")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_83() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .post(baseUrlOfSut + "/api/v4/projects/783/housekeeping")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_84() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/193?with_custom_attributes=false")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_85() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/639/forks?" + 
                    "simple=true&" + 
                    "statistics=true&" + 
                    "with_custom_attributes=true&" + 
                    "with_issues_enabled=true")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
    }
    
    
    @Test
    public void test_86() throws Exception {
        ExpectationHandler expectationHandler = expectationHandler();
        
        ValidatableResponse res_0 = given().accept("*/*")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .contentType("application/json")
                .body(" { " + 
                    " \"group_access\": 40, " + 
                    " \"group_id\": 430 " + 
                    " } ")
                .post(baseUrlOfSut + "/api/v4/projects/136/share?" + 
                    "group_id=430&" + 
                    "group_access=42")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_0.extract().statusCode()));
        
        ValidatableResponse res_1 = given().accept("application/json")
                .header("Authorization", "Bearer 2675F2yykCttcDZBz9px") // Fixed Headers
                .header("a", "a") // Fixed Headers
                .header("b", "b") // Fixed Headers
                .get(baseUrlOfSut + "/api/v4/projects/543/repository/commits/VT8BMm5ok/refs?type=all")
                .then()
                .statusCode(404)
                .assertThat()
                .contentType("application/json")
                .body("'message'", containsString("404 Project Not Found"));
        
        expectationHandler.expect(ems)
            /*
             Note: No supported codes appear to be defined. https://swagger.io/docs/specification/describing-responses/.
             This is somewhat unexpected, so the code below is likely to lead to a failed expectation
            */
            .that(sco, Arrays.asList().contains(res_1.extract().statusCode()));
    }


}
